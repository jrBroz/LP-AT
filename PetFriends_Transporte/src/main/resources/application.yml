server:
  port: 8082

spring:
  application:
    name: petfriends-transporte
  datasource:
    url: jdbc:postgresql://localhost:5434/transporte_db
    username: petfriends
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
