server:
  port: 8080

spring:
  application:
    name: petfriends-pedidos
  datasource:
    url: jdbc:postgresql://localhost:5432/pedidos_db
    username: petfriends
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
